mod main

type User {
  name:str
}

fn (u:User) greet(prefix:str) -> str {
  return "${prefix} ${u.name}"
}

fn main() -> i32 !io {
  let u = User { name : "kiro" }
  println ( u.greet ( "hi" ) )
  return 0
}
