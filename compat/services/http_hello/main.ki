mod main

import http

fn handler(req:http.Req) -> R[http.Resp,str] {
  when req.path
  "/" => {
  return Ok ( http.text ( 200 , "hello" ) )
  }
  _ => {
  return Ok ( http.not_found ( ) )
  }
}

fn main() -> i32 !net {
  http.serve ( ":8080" , handler ) ?
  return 0
}
