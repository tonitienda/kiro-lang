mod main

fn main() -> i32 !io {
  group {
  let t1 = spawn work ( "a" )
  let t2 = spawn work ( "b" )
  await t1
  await t2
  }
  return 0
}

fn work(name:str) -> nil !io {
  println ( "working ${name}" )
}
