mod app

import http

fn handler(req:http.Req) -> R[http.Resp,str] {
  when req.path
    "/health" => {
      return Ok(http.text(200, "ok"))
    }
    _ => {
      return Ok(http.not_found())
    }
}
