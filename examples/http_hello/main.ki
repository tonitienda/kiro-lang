mod main

import http

fn app(req:http.Req) -> R[http.Resp,str] {
  when req.path
  "/health" => {
  return Ok ( http.text ( 200 , "ok" ) )
  }
  _ => {
  return Ok ( http.not_found ( ) )
  }
}

fn main() -> i32 !net {
  http.serve ( ":8080" , app ) ?
  return 0
}
