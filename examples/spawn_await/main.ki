mod main

fn work(x:i32) -> i32 {
  return x + 1
}

fn main() -> i32 !io {
  let a = spawn work ( 1 )
  let b = spawn work ( 2 )
  let ra = await a
  let rb = await b
  println ( "${ra}" )
  println ( "${rb}" )
  return 0
}
