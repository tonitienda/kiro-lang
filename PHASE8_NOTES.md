# Phase 8 Notes - Templates, Examples, and Workflow Coherence

## What changed

- Templates/examples now align with real `kiro build`, `kiro run`, and `kiro test` commands rather than placeholder stubs.
- Service validation now includes handler tests and release-style smoke expectations.
- The generated-Go inspection workflow remains first-class alongside the new standalone execution path.

## Ongoing constraint

Template/example coherence still matters more than adding broad new language surface. The standalone runtime path is pragmatic and intentionally scoped around current template/service flows.
